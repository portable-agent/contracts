openapi: 3.1.0
info:
  title: Portable Agent Channel Gateway API
  version: 2.4.0
  description: Единый текстовый вход для независимых каналов пользователя.
  license:
    name: Apache-2.0
    identifier: Apache-2.0
servers:
  - url: https://api.portable-agent.dev/channel-gateway
security:
  - bearerAuth: []
paths:
  /api/v1/messages:
    post:
      tags: [messages]
      operationId: createMessage
      summary: Обработать текстовое сообщение
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageRequest"
      responses:
        "200":
          description: Предложение действия или вопрос для уточнения
          content:
            application/json:
              schema:
                $ref: "./agent-runtime-api.yaml#/components/schemas/ProposalResponse"
        "401":
          description: JWT отсутствует или не подходит Channel Gateway
        "422":
          description: Запрос не соответствует контракту
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "502":
          description: Agent Runtime временно недоступен
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
  /api/v1/conversations/messages:
    post:
      tags: [messages]
      operationId: createConversationMessage
      summary: Обработать сообщение с сохранением диалога
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "./conversation-api.yaml#/components/schemas/MessageRequest"
      responses:
        "200":
          description: Вопрос для уточнения или карточка подтверждения
          content:
            application/json:
              schema:
                $ref: "./conversation-api.yaml#/components/schemas/MessageResponse"
        "401":
          description: JWT отсутствует или не подходит Channel Gateway
        "422":
          description: Запрос не соответствует контракту
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
        "502":
          description: Conversation Service временно недоступен
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Problem"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    MessageRequest:
      type: object
      additionalProperties: false
      required: [requestKey, text, context]
      properties:
        requestKey:
          type: string
          minLength: 1
          maxLength: 200
          pattern: "^[A-Za-z0-9._:-]+$"
        text:
          type: string
          minLength: 1
          maxLength: 10000
        context:
          $ref: "#/components/schemas/MessageContext"
    MessageContext:
      type: object
      additionalProperties: false
      required: [locale, timeZone]
      properties:
        locale:
          type: string
          minLength: 2
          maxLength: 16
        timeZone:
          type: string
          minLength: 1
          maxLength: 100
          pattern: "^(UTC|[A-Za-z_]+(?:/[A-Za-z0-9_+-]+)+)$"
    Problem:
      type: object
      additionalProperties: true
      required: [detail]
      properties:
        detail:
          description: Короткое описание ошибки без внутренних данных сервиса.
